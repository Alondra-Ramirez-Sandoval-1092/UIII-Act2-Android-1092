import 'package:flutter/material.dart';

const primaryColor = Color(0xFF9966CC);       // Morado amatista
const secondaryColor = Color(0xFF98FF98);     // Verde menta
const backgroundColor1 = Color.fromARGB(255, 227, 227, 231);   // Fondo claro
const backgroundColor2 = Color.fromARGB(255, 193, 192, 194);   // Fondo más suave aún
const lightSecondary = Color(0xFFD1FFD1);     // Verde menta claro (para hover o efectos sutiles)
